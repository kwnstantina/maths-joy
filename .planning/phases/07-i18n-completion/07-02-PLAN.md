---
phase: 07-i18n-completion
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/routes/qa._index.tsx
  - app/routes/qa.$questionId.tsx
  - app/routes/qa.ask.tsx
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "All three Q&A routes (qa._index, qa.$questionId, qa.ask) export `handle = { i18n: ['common'] }`"
    - "Relative-time labels in qa.$questionId render in Greek when lang=el and English when lang=en (via Intl.RelativeTimeFormat)"
    - "Server-returned action errors in qa.$questionId and qa.ask are error KEYS (e.g., 'qa.errors.cannotVoteOwn'), and the client renders them via t()"
    - "Inline t() fallback strings (the 2-arg form t('key', 'fallback')) are removed from all three routes"
  artifacts:
    - path: "app/routes/qa._index.tsx"
      provides: "Q&A list route with handle.i18n declaration"
      contains: "export const handle"
    - path: "app/routes/qa.$questionId.tsx"
      provides: "Q&A detail route with Intl.RelativeTimeFormat + error-key returns"
      contains: "Intl.RelativeTimeFormat"
    - path: "app/routes/qa.ask.tsx"
      provides: "Q&A ask route with error-key returns"
      contains: "qa.errors."
  key_links:
    - from: "app/routes/qa.$questionId.tsx action"
      to: "client render of actionData.error"
      via: "return json({ error: 'qa.errors.cannotVoteOwn' }) → {t(actionData.error)}"
      pattern: "qa\\.errors\\."
    - from: "app/routes/qa.$questionId.tsx formatRelativeTime"
      to: "Intl.RelativeTimeFormat(locale)"
      via: "locale passed from loader + useTranslation().i18n.language"
      pattern: "Intl\\.RelativeTimeFormat"
---

<objective>
Retrofit all three Q&A routes to complete their i18n coverage: add missing handle declarations, replace hardcoded English in `formatRelativeTime` with the native `Intl.RelativeTimeFormat` browser API, convert server-returned action error strings into error KEYS that the client renders through `t()`, and remove the 2-arg `t('key', 'fallback')` calls now that 07-01 has added all the keys.

Purpose: Close Findings 2, 3b, and 3c from 07-RESEARCH.md. After this plan, the entire Q&A user surface switches cleanly between EL and EN — including time-ago labels and the ~16 error messages returned from action handlers.
Output: Three updated route files. Zero new JSON keys (07-01 already added them all). Zero new components.
</objective>

<execution_context>
@/home/konstantina/.claude/get-shit-done/workflows/execute-plan.md
@/home/konstantina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-i18n-completion/07-RESEARCH.md
@.planning/phases/07-i18n-completion/07-01-PLAN.md
@app/routes/qa._index.tsx
@app/routes/qa.$questionId.tsx
@app/routes/qa.ask.tsx

<interfaces>
<!-- Error-key contract — established by Plan 07-01 -->
<!-- Server actions return: json<ActionData>({ error: 'qa.errors.XYZ' }, { status }) -->
<!-- Client renders: {actionData?.error && <p>{t(actionData.error)}</p>} -->

Available error keys added by 07-01 (use these EXACT strings in action returns):
- qa.errors.questionIdRequired          → for missing questionId
- qa.errors.mustBeLoggedIn              → for unauthenticated users
- qa.errors.answerTooShort              → answer < 10 chars
- qa.errors.cannotVoteOwn               → self-vote attempt
- qa.errors.onlyAuthorCanAccept         → non-author tries to accept
- qa.errors.onlyAuthorCanDeleteQuestion → non-author tries to delete question
- qa.errors.onlyAuthorCanDeleteAnswer   → non-author tries to delete answer
- qa.errors.onlyAuthorCanEditQuestion   → non-author tries to edit question
- qa.errors.onlyAuthorCanEditAnswer     → non-author tries to edit answer
- qa.errors.titleTooShort               → title < 10 chars
- qa.errors.titleTooLong                → title > 200 chars
- qa.errors.bodyTooShort                → ask-question body < 30 chars (qa.ask.tsx)
- qa.errors.editBodyTooShort            → edit-body < 10 chars (qa.$questionId.tsx edit path)
- qa.errors.selectCategory              → no category selected
- qa.errors.unknownAction               → unknown _action value
- qa.errors.createFailed                → question create threw
- qa.errors.generic                     → catch-all error

Available UI keys added by 07-01 (already referenced by 2-arg t() calls today):
- qa.edit, qa.save, qa.edited, qa.tagsPlaceholder
- qa.deleteQuestionTitle, qa.deleteQuestionMessage
- qa.deleteAnswerTitle, qa.deleteAnswerMessage

Intl.RelativeTimeFormat usage (native browser API, no new dependency):
```typescript
const rtf = new Intl.RelativeTimeFormat(
  i18n.language?.startsWith('en') ? 'en' : 'el',
  { numeric: 'auto' }
);
rtf.format(-2, 'day');   // EL: "προχθές" | EN: "2 days ago"
rtf.format(-3, 'hour');  // EL: "πριν από 3 ώρες" | EN: "3 hours ago"
```

Pattern for locale selection in a Remix client component:
```typescript
const { t, i18n } = useTranslation();
const lang = i18n.language?.startsWith('en') ? 'en' : 'el';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add handle.i18n to all three Q&A routes and strip 2-arg t() fallbacks</name>
  <files>app/routes/qa._index.tsx, app/routes/qa.$questionId.tsx, app/routes/qa.ask.tsx</files>
  <action>
**In each of the three files**, add this export (placed after the import block, before the `interface` / `loader` declarations):

```typescript
export const handle = { i18n: ["common"] };
```

**In `app/routes/qa.$questionId.tsx`** — replace every 2-arg `t('key', 'fallback')` call with the 1-arg form `t('key')`. The fallback keys now exist in both locale files (added by 07-01), so the fallback is dead code. Specific lines from 07-RESEARCH.md Finding 1:
- Line 441: `t('qa.tagsPlaceholder', 'Comma-separated tags')` → `t('qa.tagsPlaceholder')`
- Line 453: `t('qa.save', 'Save')` → `t('qa.save')`
- Line 497: `t('qa.edited', 'edited')` → `t('qa.edited')`
- Line 509: `t('qa.edit', 'Edit')` → `t('qa.edit')`
- Line 607: `t('qa.save', 'Save')` → `t('qa.save')`
- Line 629: `t('qa.edited', 'edited')` → `t('qa.edited')`
- Line 640: `t('qa.edit', 'Edit')` → `t('qa.edit')`
- Line 716: `t('qa.deleteQuestionTitle', 'Delete Question')` → `t('qa.deleteQuestionTitle')`
- Line 717: `t('qa.deleteQuestionMessage', '…')` → `t('qa.deleteQuestionMessage')`
- Line 734: `t('qa.deleteAnswerTitle', 'Delete Answer')` → `t('qa.deleteAnswerTitle')`
- Line 735: `t('qa.deleteAnswerMessage', '…')` → `t('qa.deleteAnswerMessage')`

(Use Grep to scan for any other `t\('[^']+',\s*'` patterns in the three files and strip the second arg the same way. These masked-fallback calls are a red flag per RESEARCH Pitfall 1.)

**Do NOT modify** `components/qa/ConfirmModal.tsx` or `components/qa/VoteButtons.tsx`. Per RESEARCH §07-02 recommendation, those are already translated correctly at their call sites (callers always pass translated values). Scope them out.

**Out of scope check (orphan audit, per orchestrator notes):** run `grep -r "components/qa/ConfirmModal" app/ components/` before editing; confirm it is imported by `qa.$questionId.tsx` and nothing else needs a cross-check. Mark any `components/uploadExTabs/*`, `components/training/trainingList.tsx`, `components/kbar/kbar.tsx` encountered during Grep as explicitly OUT OF SCOPE for Phase 7 (they are orphans per 07-RESEARCH Finding 5 — verified by grep: no app/ route imports them).
  </action>
  <verify>
    <automated>grep -n "export const handle = { i18n:" app/routes/qa._index.tsx app/routes/qa.\$questionId.tsx app/routes/qa.ask.tsx && ! grep -rn "t('qa\.[a-zA-Z]*',\s*'" app/routes/qa.\$questionId.tsx</automated>
  </verify>
  <done>All three files export `handle = { i18n: ["common"] }`. No 2-arg `t('qa.*', '...')` calls remain in `qa.$questionId.tsx`.</done>
</task>

<task type="auto">
  <name>Task 2: Replace formatRelativeTime with Intl.RelativeTimeFormat in qa.$questionId.tsx</name>
  <files>app/routes/qa.$questionId.tsx</files>
  <action>
In `app/routes/qa.$questionId.tsx`, find the `formatRelativeTime` function (currently at lines ~332-345 per 07-RESEARCH Finding 3b):

```typescript
const formatRelativeTime = (dateString: string) => {
  const now = Date.now();
  const then = new Date(dateString).getTime();
  const diffMs = now - then;
  const diffSec = Math.floor(diffMs / 1000);
  const diffMin = Math.floor(diffSec / 60);
  const diffHr = Math.floor(diffMin / 60);
  const diffDay = Math.floor(diffHr / 24);

  if (diffDay > 0) return `${diffDay} day${diffDay > 1 ? 's' : ''} ago`;
  if (diffHr > 0) return `${diffHr} hour${diffHr > 1 ? 's' : ''} ago`;
  if (diffMin > 0) return `${diffMin} minute${diffMin > 1 ? 's' : ''} ago`;
  return 'just now';
};
```

**Replace with a locale-aware version using `Intl.RelativeTimeFormat`**:

```typescript
const formatRelativeTime = (dateString: string) => {
  const lang = i18n.language?.startsWith('en') ? 'en' : 'el';
  const rtf = new Intl.RelativeTimeFormat(lang, { numeric: 'auto' });
  const now = Date.now();
  const then = new Date(dateString).getTime();
  const diffMs = now - then;
  const diffSec = Math.floor(diffMs / 1000);
  const diffMin = Math.floor(diffSec / 60);
  const diffHr = Math.floor(diffMin / 60);
  const diffDay = Math.floor(diffHr / 24);

  if (diffDay > 0) return rtf.format(-diffDay, 'day');
  if (diffHr > 0) return rtf.format(-diffHr, 'hour');
  if (diffMin > 0) return rtf.format(-diffMin, 'minute');
  return rtf.format(-diffSec, 'second');
};
```

**Notes:**
- `i18n` is already destructured from `useTranslation()` (line 291: `const { t } = useTranslation();`). UPDATE that destructure to `const { t, i18n } = useTranslation();` so `i18n.language` is available inside the component.
- `Intl.RelativeTimeFormat` is a native browser API (no new dependency). Supported by every browser Remix 2 targets.
- `numeric: 'auto'` gives Greek "χθες" / "προχθές" (EL) and English "yesterday" / "2 days ago" (EN) where appropriate, and falls back to numeric ("πριν από X μέρες" / "X days ago") otherwise.
- No new locale keys needed — confirmed by orchestrator decision. Do NOT add `qa.time.*` keys.
  </action>
  <verify>
    <automated>grep -n "Intl.RelativeTimeFormat" app/routes/qa.\$questionId.tsx && grep -n "const { t, i18n } = useTranslation" app/routes/qa.\$questionId.tsx</automated>
  </verify>
  <done>`formatRelativeTime` uses `Intl.RelativeTimeFormat(lang, { numeric: 'auto' })`. The function reads `i18n.language` from `useTranslation()`. The old manual string concatenation (`${diffDay} day${diffDay > 1 ? 's' : ''} ago`) is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Convert Q&A action errors to error-KEY returns in qa.$questionId.tsx and qa.ask.tsx</name>
  <files>app/routes/qa.$questionId.tsx, app/routes/qa.ask.tsx</files>
  <action>
**Server side — replace every hardcoded English error string in the action() with the matching key from 07-01.**

In `app/routes/qa.$questionId.tsx` action handler (per 07-RESEARCH Finding 3c, lines ~100-284):

| Current string                                           | Replace with KEY                         |
|----------------------------------------------------------|------------------------------------------|
| `'Question ID required'`                                 | `'qa.errors.questionIdRequired'`         |
| `'You must be logged in'`                                | `'qa.errors.mustBeLoggedIn'`             |
| `'Answer must be at least 10 characters'` (x2 — lines 123 + 266) | `'qa.errors.answerTooShort'`      |
| `'Cannot vote on own content'` (x2 — lines 143 + 160)    | `'qa.errors.cannotVoteOwn'`              |
| `'Only question author can accept answers'`              | `'qa.errors.onlyAuthorCanAccept'`        |
| `'Only question author can delete'`                      | `'qa.errors.onlyAuthorCanDeleteQuestion'`|
| `'Only answer author can delete'`                        | `'qa.errors.onlyAuthorCanDeleteAnswer'`  |
| `'Only question author can edit'`                        | `'qa.errors.onlyAuthorCanEditQuestion'`  |
| `'Title must be at least 10 characters'`                 | `'qa.errors.titleTooShort'`              |
| `'Body must be at least 10 characters'` (edit path, line 250-251) | `'qa.errors.editBodyTooShort'`  |
| `'Only answer author can edit'`                          | `'qa.errors.onlyAuthorCanEditAnswer'`    |
| `'Unknown action'`                                       | `'qa.errors.unknownAction'`              |
| `'An error occurred'`                                    | `'qa.errors.generic'`                    |

In `app/routes/qa.ask.tsx` action handler (lines ~87-99, 123):

| Current string                                              | Replace with KEY                |
|-------------------------------------------------------------|---------------------------------|
| `'Title must be at least 10 characters'` (line 87)          | `'qa.errors.titleTooShort'`     |
| `'Title must be less than 200 characters'` (line 90)        | `'qa.errors.titleTooLong'`      |
| `'Question body must be at least 30 characters'` (line 94)  | `'qa.errors.bodyTooShort'`      |
| `'Please select a category'` (line 98)                      | `'qa.errors.selectCategory'`    |
| `'Failed to create question'` (line 123)                    | `'qa.errors.createFailed'`      |

**Client side — wrap actionData.error through t() at display sites.**

`qa.$questionId.tsx`: search the JSX for `actionData.error` or `actionData?.error` renders. If any currently does `{actionData.error}`, change to `{t(actionData.error)}`. If the error is currently not displayed anywhere user-visible (i.e., only logged server-side), NO client change is needed — but verify via Grep first.

`qa.ask.tsx` (line ~164): the current JSX shows `{actionData.error}` raw inside `<div className="mb-6 p-4 bg-red-50 …">`. Change to `{t(actionData.error)}`.

Also, `qa.ask.tsx` renders `fieldErrors.title`, `fieldErrors.body`, `fieldErrors.category` raw. Update the action to return KEY strings for `fieldErrors` as well (same mapping as above), and wrap `{actionData.fieldErrors.title}` → `{t(actionData.fieldErrors.title)}` on the client, same for body + category.

**Edge cases:**
- The `throw err` re-throws in voteQuestion/voteAnswer (after the `if (err.message.includes('own'))` check) stay as-is — they bubble to the outer catch and are converted to `'qa.errors.generic'` there.
- Type safety: if `ActionData.error` is typed `string | undefined`, this continues to work — `t(undefined as any)` returns `undefined`, but the conditional render (`actionData?.error && {t(actionData.error)}`) short-circuits before that.

**Verification (during execution):** after edits, grep the action() blocks for remaining bare-English strings:

```bash
grep -nE "error:\s*'[A-Z]" app/routes/qa.\$questionId.tsx app/routes/qa.ask.tsx
```

Should return zero matches (all `error:` values start with lowercase `qa.errors.…`).
  </action>
  <verify>
    <automated>! grep -nE "error:\s*'[A-Z]" app/routes/qa.\$questionId.tsx app/routes/qa.ask.tsx && grep -c "qa.errors." app/routes/qa.\$questionId.tsx app/routes/qa.ask.tsx</automated>
  </verify>
  <done>Every `json<ActionData>({ error: '…' })` in both files uses a `qa.errors.*` key from 07-01. Client-side `{actionData.error}` renders are wrapped as `{t(actionData.error)}`. `npm run typecheck` still passes.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes (no new type errors introduced).
- `npm run build` completes (client + server bundles compile).
- Manual EL → EN toggle on `/qa`, `/qa/:id`, `/qa/ask`:
  1. All visible text switches.
  2. No raw key strings (e.g., `qa.edit` literal) appear on screen.
  3. Force an error path (e.g., try to vote on own question, submit a too-short answer); the error message appears in the currently-selected language.
  4. The "edited X ago" indicator reads naturally in both languages.
- `grep -rn "t('[a-z.]*',\s*'[^']*')" app/routes/qa.` returns zero matches (no more 2-arg t() fallbacks).
</verification>

<success_criteria>
- Three route files (qa._index, qa.$questionId, qa.ask) each export `handle = { i18n: ["common"] }`.
- `formatRelativeTime` in qa.$questionId uses `Intl.RelativeTimeFormat`; no manual English-only string concatenation remains.
- All server-returned error strings in both action handlers are `qa.errors.*` keys.
- Client renders action errors via `t(actionData.error)` (and the matching `t(actionData.fieldErrors.x)` in qa.ask.tsx).
- No 2-arg `t('key', 'fallback')` calls remain in the three files.
- Does NOT modify any `public/locales/*/common.json` file.
- Does NOT modify `components/qa/*` files (out of scope).
</success_criteria>

<output>
After completion, create `.planning/phases/07-i18n-completion/07-02-SUMMARY.md` documenting:
- Lines changed in each of the three files.
- Full mapping of replaced error strings → error keys.
- Confirmation that Intl.RelativeTimeFormat works in EL and EN (spot-check output).
- Orphan check result: `components/uploadExTabs/*` + `components/training/trainingList.tsx` + `components/kbar/kbar.tsx` confirmed OUT OF SCOPE (not imported by any `app/` route in the grep during execution).
- Decision: Q&A default-category seeds (`['Algebra', 'Geometry', …]`) left untranslated per orchestrator note (user-generated-content boundary).
</output>
