---
phase: 07-i18n-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/locales/el/common.json
  - public/locales/en/common.json
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Every t('namespace.key') callsite used by Q&A, Exercises, Admin Videos, Admin Training, Admin Q&A has a matching key in BOTH public/locales/el/common.json AND public/locales/en/common.json"
    - "Both locale JSON files remain symmetric: el_keys ≡ en_keys (no one-sided additions)"
    - "Both files remain valid JSON (parseable, no trailing commas, no duplicate keys)"
  artifacts:
    - path: "public/locales/el/common.json"
      provides: "Greek translations for all v1.1 keys"
      contains: "qa.edit"
    - path: "public/locales/en/common.json"
      provides: "English translations for all v1.1 keys"
      contains: "qa.edit"
  key_links:
    - from: "public/locales/el/common.json"
      to: "public/locales/en/common.json"
      via: "symmetric key sets"
      pattern: "qa\\.errors\\.|qa\\.time\\.|qa\\.edit|qa\\.save|qa\\.edited|qa\\.tagsPlaceholder|qa\\.deleteQuestion|qa\\.deleteAnswer|admin\\.qa\\.actions|admin\\.training\\.replaceContentImage|admin\\.training\\.replaceSolutionImage|exercises\\.download|exercises\\.pdfNotAvailable"
---

<objective>
Add every locale key that Plans 07-02, 07-03, 07-04 will consume. This is the foundation plan (wave 1) — the three feature plans all depend on it so they can safely edit code without touching the locale JSON files.

Purpose: Centralize all key additions into a single plan so that 07-02/03/04 can run in parallel without any JSON-file contention. Eliminates the "missing-key masking" pitfall from RESEARCH Finding 1 by ensuring every key exists in BOTH locale files before any code uses it.
Output: Updated `public/locales/el/common.json` and `public/locales/en/common.json`, both still valid JSON and still symmetric.
</objective>

<execution_context>
@/home/konstantina/.claude/get-shit-done/workflows/execute-plan.md
@/home/konstantina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-i18n-completion/07-RESEARCH.md
@public/locales/el/common.json
@public/locales/en/common.json

<interfaces>
<!-- Existing key conventions in the file (from locale JSON inspection) -->
<!-- Keys use dot-namespace strings at the top level, NOT nested objects. -->
<!-- Example: "qa.edit": "Edit" (NOT "qa": { "edit": "Edit" }). -->
<!-- Both files are single-level flat dictionaries; preserve that shape. -->

From public/locales/el/common.json (top-level flat dict, ~478 keys, Greek):
```json
{
  "title": "Ασκήσεις και θέματα μαθηματικών.",
  "qa.title": "Ερωτήσεις & Απαντήσεις",
  "qa.edit": "<<NEEDS ADD>>",
  ...
}
```

From public/locales/en/common.json (top-level flat dict, ~478 keys, English):
```json
{
  "title": "Math exercises and topics.",
  "qa.title": "Questions & Answers",
  "qa.edit": "<<NEEDS ADD>>",
  ...
}
```

Existing keys that ALREADY exist (do NOT re-add — just confirm with Grep):
- `admin.videos.uploading`, `admin.videos.uploadButton` (present in both; Plan 07-03 will reuse these)
- `qa.confirmDeleteQuestion`, `qa.confirmDeleteAnswer` (present; but 07-02 wants separate `qa.deleteQuestionTitle/Message` + `qa.deleteAnswerTitle/Message` for ConfirmModal — ADD the new ones anyway; the existing keys stay for now)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all missing UI-text keys to both locale files</name>
  <files>public/locales/el/common.json, public/locales/en/common.json</files>
  <action>
Add the following keys to BOTH `public/locales/el/common.json` AND `public/locales/en/common.json`. Keep the flat dot-namespace structure (no nesting). Insert in logical groups:

**Group A — Q&A inline UI keys (8 keys)** — add near existing `qa.*` block around line 155:
- `qa.edit` — EL: `Επεξεργασία` | EN: `Edit`
- `qa.save` — EL: `Αποθήκευση` | EN: `Save`
- `qa.edited` — EL: `επεξεργάστηκε` | EN: `edited`
- `qa.tagsPlaceholder` — EL: `Ετικέτες χωρισμένες με κόμμα` | EN: `Comma-separated tags`
- `qa.deleteQuestionTitle` — EL: `Διαγραφή Ερώτησης` | EN: `Delete Question`
- `qa.deleteQuestionMessage` — EL: `Είστε σίγουροι ότι θέλετε να διαγράψετε αυτή την ερώτηση; Η ενέργεια αυτή δεν μπορεί να αναιρεθεί.` | EN: `Are you sure you want to delete this question? This action cannot be undone.`
- `qa.deleteAnswerTitle` — EL: `Διαγραφή Απάντησης` | EN: `Delete Answer`
- `qa.deleteAnswerMessage` — EL: `Είστε σίγουροι ότι θέλετε να διαγράψετε αυτή την απάντηση; Η ενέργεια αυτή δεν μπορεί να αναιρεθεί.` | EN: `Are you sure you want to delete this answer? This action cannot be undone.`

**Group B — Q&A action-error keys (17 keys, for Plan 07-02 server-returned error-key pattern)** — add as a grouped sub-namespace `qa.errors.*`:
- `qa.errors.questionIdRequired` — EL: `Απαιτείται αναγνωριστικό ερώτησης` | EN: `Question ID required`
- `qa.errors.mustBeLoggedIn` — EL: `Πρέπει να είστε συνδεδεμένοι` | EN: `You must be logged in`
- `qa.errors.answerTooShort` — EL: `Η απάντηση πρέπει να έχει τουλάχιστον 10 χαρακτήρες` | EN: `Answer must be at least 10 characters`
- `qa.errors.cannotVoteOwn` — EL: `Δεν μπορείτε να ψηφίσετε το δικό σας περιεχόμενο` | EN: `Cannot vote on own content`
- `qa.errors.onlyAuthorCanAccept` — EL: `Μόνο ο συγγραφέας της ερώτησης μπορεί να αποδεχτεί απαντήσεις` | EN: `Only the question author can accept answers`
- `qa.errors.onlyAuthorCanDeleteQuestion` — EL: `Μόνο ο συγγραφέας της ερώτησης μπορεί να τη διαγράψει` | EN: `Only the question author can delete it`
- `qa.errors.onlyAuthorCanDeleteAnswer` — EL: `Μόνο ο συγγραφέας της απάντησης μπορεί να τη διαγράψει` | EN: `Only the answer author can delete it`
- `qa.errors.onlyAuthorCanEditQuestion` — EL: `Μόνο ο συγγραφέας της ερώτησης μπορεί να την επεξεργαστεί` | EN: `Only the question author can edit it`
- `qa.errors.onlyAuthorCanEditAnswer` — EL: `Μόνο ο συγγραφέας της απάντησης μπορεί να την επεξεργαστεί` | EN: `Only the answer author can edit it`
- `qa.errors.titleTooShort` — EL: `Ο τίτλος πρέπει να έχει τουλάχιστον 10 χαρακτήρες` | EN: `Title must be at least 10 characters`
- `qa.errors.titleTooLong` — EL: `Ο τίτλος πρέπει να έχει λιγότερους από 200 χαρακτήρες` | EN: `Title must be less than 200 characters`
- `qa.errors.bodyTooShort` — EL: `Η ερώτηση πρέπει να έχει τουλάχιστον 30 χαρακτήρες` | EN: `Question body must be at least 30 characters`
- `qa.errors.editBodyTooShort` — EL: `Το σώμα πρέπει να έχει τουλάχιστον 10 χαρακτήρες` | EN: `Body must be at least 10 characters`
- `qa.errors.selectCategory` — EL: `Παρακαλώ επιλέξτε κατηγορία` | EN: `Please select a category`
- `qa.errors.unknownAction` — EL: `Άγνωστη ενέργεια` | EN: `Unknown action`
- `qa.errors.createFailed` — EL: `Αποτυχία δημιουργίας ερώτησης` | EN: `Failed to create question`
- `qa.errors.generic` — EL: `Προέκυψε σφάλμα` | EN: `An error occurred`

**Group C — Admin-Q&A key (1 key)** — add near existing `admin.qa.*` block:
- `admin.qa.actions` — EL: `Ενέργειες` | EN: `Actions`

**Group D — Admin-Training keys (2 keys)** — add near existing `admin.training.*` block:
- `admin.training.replaceContentImage` — EL: `Αντικατάσταση Εικόνας Περιεχομένου` | EN: `Replace Content Image`
- `admin.training.replaceSolutionImage` — EL: `Αντικατάσταση Εικόνας Λύσης` | EN: `Replace Solution Image`

**Group E — Exercise public PDF viewer keys (2 keys)** — add near existing `exercises.*` block around line 206:
- `exercises.download` — EL: `Λήψη` | EN: `Download`
- `exercises.pdfNotAvailable` — EL: `Το PDF δεν είναι διαθέσιμο` | EN: `PDF not available`

**What NOT to add** (decisions from orchestrator notes):
- Do NOT add `admin.videos.createButton` or `admin.videos.creating` — Plan 07-03 will instead swap the component to use the EXISTING `admin.videos.uploadButton` / `admin.videos.uploading` keys.
- Do NOT add Q&A category seed keys (`qa.categories.algebra`, etc.) — per orchestrator decision, default-category seeds (Algebra, Geometry, …) are scoped as user-generated content and stay untranslated.
- Do NOT add `qa.time.*` relative-time keys — Plan 07-02 will replace `formatRelativeTime` with `Intl.RelativeTimeFormat`, which needs no locale keys.

**Insertion rules:**
- Preserve existing key order; append new keys at the end of their logical section (or at the end of the file if no clear section).
- Both files MUST end up with the same set of keys. Add the same 30 keys to both (8 + 17 + 1 + 2 + 2 = 30, counting Group B as 17 keys including `generic`/`createFailed`/`unknownAction`/`editBodyTooShort`).
- No trailing comma on the last entry in each file.
- Keep the outer `{` / `}` JSON wrapper.

**Final verification (do this at the end of the task):**
```bash
# 1. Both files still valid JSON
node -e "JSON.parse(require('fs').readFileSync('public/locales/el/common.json','utf8'))"
node -e "JSON.parse(require('fs').readFileSync('public/locales/en/common.json','utf8'))"

# 2. Both files have the same set of keys
node -e "
const el = Object.keys(JSON.parse(require('fs').readFileSync('public/locales/el/common.json','utf8')));
const en = Object.keys(JSON.parse(require('fs').readFileSync('public/locales/en/common.json','utf8')));
const elOnly = el.filter(k => !en.includes(k));
const enOnly = en.filter(k => !el.includes(k));
if (elOnly.length || enOnly.length) {
  console.error('ASYMMETRY:', { elOnly, enOnly });
  process.exit(1);
}
console.log('OK — symmetric, el =', el.length, ', en =', en.length);
"

# 3. All new keys present in both
node -e "
const REQUIRED = [
  'qa.edit','qa.save','qa.edited','qa.tagsPlaceholder',
  'qa.deleteQuestionTitle','qa.deleteQuestionMessage',
  'qa.deleteAnswerTitle','qa.deleteAnswerMessage',
  'qa.errors.questionIdRequired','qa.errors.mustBeLoggedIn',
  'qa.errors.answerTooShort','qa.errors.cannotVoteOwn',
  'qa.errors.onlyAuthorCanAccept','qa.errors.onlyAuthorCanDeleteQuestion',
  'qa.errors.onlyAuthorCanDeleteAnswer','qa.errors.onlyAuthorCanEditQuestion',
  'qa.errors.onlyAuthorCanEditAnswer','qa.errors.titleTooShort',
  'qa.errors.titleTooLong','qa.errors.bodyTooShort','qa.errors.editBodyTooShort',
  'qa.errors.selectCategory','qa.errors.unknownAction',
  'qa.errors.createFailed','qa.errors.generic',
  'admin.qa.actions',
  'admin.training.replaceContentImage','admin.training.replaceSolutionImage',
  'exercises.download','exercises.pdfNotAvailable'
];
for (const f of ['el','en']) {
  const keys = Object.keys(JSON.parse(require('fs').readFileSync('public/locales/'+f+'/common.json','utf8')));
  const missing = REQUIRED.filter(k => !keys.includes(k));
  if (missing.length) { console.error(f, 'missing:', missing); process.exit(1); }
}
console.log('OK — all required keys present in both locales');
"
```

Reference: .planning/phases/07-i18n-completion/07-RESEARCH.md Finding 1 (locale-key audit) for exact keys + suggested wording.
  </action>
  <verify>
    <automated>
node -e "const el=Object.keys(JSON.parse(require('fs').readFileSync('public/locales/el/common.json','utf8')));const en=Object.keys(JSON.parse(require('fs').readFileSync('public/locales/en/common.json','utf8')));const req=['qa.edit','qa.save','qa.edited','qa.tagsPlaceholder','qa.deleteQuestionTitle','qa.deleteQuestionMessage','qa.deleteAnswerTitle','qa.deleteAnswerMessage','qa.errors.questionIdRequired','qa.errors.mustBeLoggedIn','qa.errors.answerTooShort','qa.errors.cannotVoteOwn','qa.errors.onlyAuthorCanAccept','qa.errors.onlyAuthorCanDeleteQuestion','qa.errors.onlyAuthorCanDeleteAnswer','qa.errors.onlyAuthorCanEditQuestion','qa.errors.onlyAuthorCanEditAnswer','qa.errors.titleTooShort','qa.errors.titleTooLong','qa.errors.bodyTooShort','qa.errors.editBodyTooShort','qa.errors.selectCategory','qa.errors.unknownAction','qa.errors.createFailed','qa.errors.generic','admin.qa.actions','admin.training.replaceContentImage','admin.training.replaceSolutionImage','exercises.download','exercises.pdfNotAvailable'];const missEl=req.filter(k=>!el.includes(k));const missEn=req.filter(k=>!en.includes(k));const elOnly=el.filter(k=>!en.includes(k));const enOnly=en.filter(k=>!el.includes(k));if(missEl.length||missEn.length||elOnly.length||enOnly.length){console.error({missEl,missEn,elOnly,enOnly});process.exit(1)}if(req.length!==30){console.error('expected 30 required keys, got',req.length);process.exit(1)}console.log('OK keys=',el.length)"
    </automated>
  </verify>
  <done>Both locale files contain all 30 new keys, both files parse as valid JSON, and their key sets are symmetric (el_keys ≡ en_keys).</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck` — should still pass (no code changes).
- Run the JSON-validity + symmetry + required-key Node one-liner in the Task verify block — must exit 0.
- Manual: open both JSON files; new keys appear in logical groupings; file still ends with `}` and no trailing comma.
</verification>

<success_criteria>
- All 30 keys listed above exist in BOTH `public/locales/el/common.json` and `public/locales/en/common.json`.
- Both files still parse as valid JSON.
- Key sets are symmetric: `diff <(jq -r 'keys[]' public/locales/el/common.json | sort) <(jq -r 'keys[]' public/locales/en/common.json | sort)` is empty (or the equivalent Node.js check passes).
- No code file in the repo has been modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/07-i18n-completion/07-01-SUMMARY.md` documenting:
- Count of keys added (30 expected) per group.
- Final key count in each locale file.
- Decision NOT to add `admin.videos.createButton`/`creating` (justified: 07-03 uses existing keys instead).
- Decision NOT to add `qa.categories.*` seeds (justified: user-generated content per REQUIREMENTS).
- Decision NOT to add `qa.time.*` relative-time keys (justified: 07-02 uses `Intl.RelativeTimeFormat`).
</output>
