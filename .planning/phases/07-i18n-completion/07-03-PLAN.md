---
phase: 07-i18n-completion
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - components/admin/VideoUploadForm.tsx
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Admin video upload form Submit button text switches cleanly between EL and EN"
    - "The form uses the existing admin.videos.uploadButton / admin.videos.uploading keys (not the non-existent admin.videos.createButton / admin.videos.creating)"
    - "TrainingCard image-replace buttons render correctly in both languages (verified — keys now exist after 07-01)"
  artifacts:
    - path: "components/admin/VideoUploadForm.tsx"
      provides: "Admin video upload form with correct i18n key references"
      contains: "admin.videos.uploadButton"
  key_links:
    - from: "components/admin/VideoUploadForm.tsx Submit button"
      to: "public/locales/{el,en}/common.json admin.videos.uploadButton"
      via: "t('admin.videos.uploadButton')"
      pattern: "admin\\.videos\\.(uploadButton|uploading)"
---

<objective>
Fix the admin Video upload form so its Submit-button label actually translates. The form currently calls `t('admin.videos.creating')` / `t('admin.videos.createButton')`, but those keys exist in neither locale file. Every other admin form (Exercises, Training, Books) uses `admin.videos.uploadButton` / `admin.videos.uploading` — which DO exist in both locale files already. Swap the component to the existing convention; do NOT invent new keys.

Purpose: Close 07-RESEARCH Finding 1 (the two non-existent `createButton` / `creating` keys) with the smallest, cleanest fix — reuse the established pattern instead of adding new keys. Also verify (read-only) that `TrainingCard.tsx` now renders correctly now that 07-01 has added the two `admin.training.replaceContentImage` / `replaceSolutionImage` keys.
Output: One updated component file (`components/admin/VideoUploadForm.tsx`). Zero locale-file changes.
</objective>

<execution_context>
@/home/konstantina/.claude/get-shit-done/workflows/execute-plan.md
@/home/konstantina/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-i18n-completion/07-RESEARCH.md
@.planning/phases/07-i18n-completion/07-01-PLAN.md
@components/admin/VideoUploadForm.tsx
@components/admin/TrainingCard.tsx

<interfaces>
<!-- Existing keys that MUST be used (confirmed present in both locale files) -->

From public/locales/{el,en}/common.json:
- "admin.videos.uploading"   — e.g., EL: "Μεταφόρτωση..."  | EN: "Uploading..."
- "admin.videos.uploadButton" — e.g., EL: "Δημιουργία Μαθήματος" | EN: "Create Tutorial"

(Verified via grep at orchestrator time: both keys present at line ~428 in each locale file.)

From components/admin/TrainingCard.tsx lines 140 + 156 (read-only — NO edits by this plan):
```tsx
{t("admin.training.replaceContentImage")}
{t("admin.training.replaceSolutionImage")}
```
Both keys added by 07-01. Just verify they render after this plan ships; no component change needed.

Pattern reference — the correct convention from components/admin/ExerciseUploadForm.tsx lines 132-134:
```tsx
{isSubmitting
  ? t("admin.exercises.uploading")
  : t("admin.exercises.uploadButton")}
```
Mirror this exact pattern for videos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap VideoUploadForm to use the existing admin.videos.uploadButton / uploading keys</name>
  <files>components/admin/VideoUploadForm.tsx</files>
  <action>
In `components/admin/VideoUploadForm.tsx`, locate the Submit button (currently at lines 137-145):

```tsx
<button
  type="submit"
  disabled={isSubmitting}
  className="w-full rounded bg-orange-500 py-2 px-4 text-white hover:bg-orange-600 focus:bg-orange-400 disabled:opacity-50 disabled:cursor-not-allowed"
>
  {isSubmitting
    ? t("admin.videos.creating")
    : t("admin.videos.createButton")}
</button>
```

**Change** the two key references on lines 143-144:
- `t("admin.videos.creating")` → `t("admin.videos.uploading")`
- `t("admin.videos.createButton")` → `t("admin.videos.uploadButton")`

This is the ONLY change needed. Every other string reference in the file is already correctly translated via existing keys.

**Why this pattern:** 07-RESEARCH Finding 1 calls out two options:
- Option A: add `createButton` / `creating` as new keys.
- Option B: swap the component to use the existing `uploadButton` / `uploading` keys (the convention shared by ExerciseUploadForm, TrainingUploadForm, BookUploadForm).

Orchestrator picked Option B — it keeps the locale file small, matches the established pattern, and the human-readable strings ("Create Tutorial" / "Creating...") already exist at those keys (verified via grep).

**Do NOT** modify any other file in this plan. Specifically:
- Do NOT modify `components/admin/TrainingCard.tsx` — its `admin.training.replaceContentImage` + `replaceSolutionImage` keys were added by 07-01 and its existing code works as-is. Just confirm via `grep` that the calls at lines 140 + 156 match the keys in the locale file.
- Do NOT add any new keys to locale files (07-01 is the only plan that writes JSON).
- Do NOT touch other admin forms — they already use the correct convention.

**Orphan check (per orchestrator notes):** before making the edit, run `grep -rn "VideoUploadForm" app/ components/` to confirm this component is still used by `app/routes/admin.videos.tsx`. (It is — but include the grep result in the SUMMARY.) Also confirm that `components/uploadExTabs/uploadTutorial.tsx` is NOT referenced from any `app/routes/*` except legacy `app/routes/uploadEx.tsx` — mark OUT OF SCOPE if only the legacy route touches it.
  </action>
  <verify>
    <automated>grep -n "admin.videos.uploadButton\|admin.videos.uploading" components/admin/VideoUploadForm.tsx && ! grep -n "admin.videos.createButton\|admin.videos.creating" components/admin/VideoUploadForm.tsx</automated>
  </verify>
  <done>`components/admin/VideoUploadForm.tsx` calls `t("admin.videos.uploading")` and `t("admin.videos.uploadButton")`. The previous `creating` / `createButton` key references are gone. No other files modified.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes.
- `npm run build` completes.
- Manual check: navigate to `/admin/videos`, toggle language (EL ↔ EN):
  1. Submit button reads "Δημιουργία Μαθήματος" in EL and "Create Tutorial" in EN (from `admin.videos.uploadButton`).
  2. During submission, button reads "Μεταφόρτωση..." in EL and "Uploading..." in EN.
- Navigate to `/admin/training`, open any training card's edit mode:
  3. "Αντικατάσταση Εικόνας Περιεχομένου" (EL) / "Replace Content Image" (EN) label appears — confirms 07-01's `admin.training.*` keys wire up correctly with the existing TrainingCard code (no code change needed here).
- Plan 07-02 and 07-04 files should be untouched by this plan.
</verification>

<success_criteria>
- VideoUploadForm Submit button renders correctly in both languages.
- No changes to locale JSON files.
- No changes to other components or routes.
- File-modified count: exactly 1 (`components/admin/VideoUploadForm.tsx`).
</success_criteria>

<output>
After completion, create `.planning/phases/07-i18n-completion/07-03-SUMMARY.md` documenting:
- Exact before/after diff of the two lines changed (lines 143-144 in VideoUploadForm.tsx).
- Confirmation that TrainingCard.tsx now renders correctly with 07-01's new keys (manual/spot-check).
- Orphan audit result: confirm `components/uploadExTabs/uploadTutorial.tsx` is NOT a v1.1 admin route dependency (only legacy `app/routes/uploadEx.tsx` references it) — marked OUT OF SCOPE for Phase 7.
- Why Option B (swap to existing keys) chosen over Option A (add new keys): matches the established admin-form convention, keeps locale files small.
</output>
